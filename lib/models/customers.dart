import 'package:flutter/material.dart';

class Customers {
  String firstName;
  String lastName;
  String email;
  double balance;

  Customers(this.firstName,this.lastName, this.email, this.balance);
}
